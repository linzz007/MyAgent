# 目前工程实现文档（重新整理版）

> 本文档实时记录表格问答系统各模块的当前实现逻辑和设计细节。
> 根据用户反馈重新整理：模块2、3、4是一个整体模块（Router模块）。

最后更新时间：2025年1月

---

## 目录

1. [项目概述](#项目概述)
2. [整体架构](#整体架构)
3. [核心分数系统（sem_score & cell_score）](#核心分数系统sem_score--cell_score) ⭐
4. [核心模块详解](#核心模块详解)
5. [数据流与状态管理](#数据流与状态管理)
6. [Token消耗优化策略](#token消耗优化策略)
7. [关键设计决策](#关键设计决策)

---

## 项目概述

### 项目目标

本项目实现了一个**任务特征感知的链式推理路径选择优化方法**，核心目标是：
- **提高表格数据获取的准确性**
- **大幅减少LLM调用的token消耗**（目标：减少95%+）

### 核心创新点

1. **自适应路径选择**：根据问题难度和特征，智能选择简单路径或复杂路径
2. **表格压缩优化**：在路由阶段压缩表格，只保留需要的数据
3. **轻量校验机制**：对复杂路径结果进行基本校验，避免过度消耗

---

## 整体架构

### 系统架构图

```
用户输入 (Q, T)
    ↓
[InputHandler] 输入管理与格式统一
    ↓
[Router模块] ⭐ 核心模块：生成压缩表格 + 最终分数
    ├─→ 计算sem_score（语义难度分数）
    ├─→ 获取需要的行列信息
    ├─→ 压缩表格
    ├─→ 计算cell_score（压缩后表格大小/原表格大小）
    ├─→ 组合两个分数得到最终分数
    └─→ 路径决策 (SIMPLE/COMPLEX)
    ↓
    ├─→ SIMPLE路径
    │       ↓
    │   [SimplePathAgent] 简单路径推理
    │       ↓
    │   [FinalAnswerAgent] 生成最终答案
    │
    └─→ COMPLEX路径
            ↓
        [Planner] 复杂路径规划
            ↓
        [Calculator] 代码执行
            ↓
        [LightCritic] 轻量校验
            ↓ (如果REPLAN，返回Planner)
        [FinalAnswerAgent] 生成最终答案
```

### 模块列表

| 模块名称 | 文件位置 | 职责 | Token消耗优化作用 |
|---------|---------|------|------------------|
| InputHandler | `my_agents.py:126` | 输入预处理和格式统一 | 构建轻量schema，减少后续token |
| **Router模块** ⭐ | `my_agents.py:444-878` | **核心模块**：生成压缩表格+最终分数 | **核心优化：表格压缩97%+，路径选择减少98.9%** |
| SimplePathAgent | `my_agents.py:1274` | 简单路径推理 | **核心优化：简单问题减少98.9%** |
| PlannerAgent | `my_agents.py:928` | 复杂路径规划 | 使用压缩表格 |
| Calculator | `my_agents.py:1004` | 代码执行 | 无token消耗 |
| CriticAgent | `my_agents.py:1147` | 轻量校验 | 使用压缩表格 |
| FinalAnswerAgent | `my_agents.py:1349` | 最终答案生成 | 轻量格式化 |
| TableQAPipeline | `my_agents.py:1391` | 流程编排器 | 统一入口和状态管理 |

---

## 核心分数系统（sem_score & cell_score）⭐

> **这是本项目的核心创新点**：通过两个关键分数（sem_score和cell_score）实现任务特征感知的路径选择。

### 核心分数概述

本系统通过两个核心分数来量化问题的难度和复杂度：

1. **sem_score（语义难度分数）**：衡量问题语义层面的复杂度
2. **cell_score（结构复杂度分数）**：衡量问题需要触达的表格单元格比例

这两个分数通过加权组合得到综合难度分数，用于决定走简单路径还是复杂路径。

### 1. sem_score（语义难度分数）

**定义**：衡量问题在语义层面的复杂度，反映问题的推理难度。

**计算方式**：
- **模块**：Router模块中的语义分析步骤
- **方法**：通过LLM分析问题的语义复杂度
- **输入**：问题文本 + 表格schema（列名、预览）
- **输出**：语义难度分数 [0, 1]

**分数含义**：
- **0.0 - 0.3**：简单语义（单步查询，无需复杂推理）
- **0.3 - 0.7**：中等语义（需要2-3步推理，涉及简单运算）
- **0.7 - 1.0**：复杂语义（需要多步推理，涉及复杂计算、聚合、趋势分析）

**关键点**：
- 在计算sem_score的时候，**会同时获取哪些行列是需要用得上的**

### 2. cell_score（结构复杂度分数）

**定义**：衡量问题需要触达的表格单元格比例，反映问题的数据范围需求。

**计算方式**：
- **模块**：Router模块中的表格压缩步骤
- **计算逻辑**：
  1. 在计算sem_score时，获取需要的行列信息（sel_rows, sel_cols）
  2. 用这些行列信息压缩表格，生成新的表格
  3. **cell_score = 压缩后表格大小 / 原表格大小**
  
**计算公式**：
```python
# 压缩后表格大小
compressed_size = len(sel_rows) × len(sel_cols)  # 压缩后的行列数

# 原表格大小
original_size = original_rows × original_cols  # 原表格的行列数

# cell_score
cell_score = compressed_size / original_size  # 范围 [0, 1]
```

**分数含义**：
- **0.0 - 0.2**：小范围数据（只需少量单元格）
- **0.2 - 0.5**：中等范围数据（需要部分数据）
- **0.5 - 1.0**：大范围数据（需要大部分或全部数据）

**关键点**：
- **cell_score就是压缩后表格的大小比例**
- 压缩后的表格就是后续模块使用的表格

### 3. 分数组合：综合难度计算

**组合方式**：加权线性组合

**公式**：
```python
total_score = w_sem * sem_score + w_cell * cell_score
```

**权重配置**（默认值）：
- `w_sem = 0.6`：语义分数权重（60%）
- `w_cell = 0.4`：结构复杂度分数权重（40%）

**难度等级划分**：
```python
if total_score < threshold (0.5):
    difficulty_level = "easy"  # 简单路径
else:
    difficulty_level = "hard"  # 复杂路径
```

---

## 核心模块详解

### 1. InputHandler（输入管理模块）

**文件位置**：`code/my_agents.py:126-224`

**职责**：
- 接收原始输入：自然语言问题 Q，以及表格 T（CSV / JSON / pandas.DataFrame）
- 负责基本预处理、格式统一
- 构建轻量级的表格schema（而非完整表格）

**核心方法**：

#### `process_input(question: str, table: Any) -> Tuple[str, pd.DataFrame, Dict[str, Any]]`

**实现逻辑**：
1. 规范化问题文本（去除首尾空白）
2. 将表格统一转换为 pandas.DataFrame
3. 构建表格schema（包含列名、预览文本、行列数等）

**输出格式**：
```python
(
    normalized_question: str,  # 规范化的问题文本
    df: pd.DataFrame,          # 统一的表格数据
    table_schema: Dict {       # 轻量schema
        "columns": List[str],      # 列名列表
        "preview_text": str,       # 前5行的文本预览
        "num_rows": int,           # 总行数
        "num_cols": int            # 总列数
    }
)
```

---

### 2. Router模块（路由与表格压缩模块）⭐⭐

> **核心模块**：模块2、3、4实际上是一个整体模块，统称为 Router模块。
> 这个模块的最终目的是：**生成压缩后的表格 + 生成最终分数（由sem_score和cell_score共同决定）**

**文件位置**：
- FeatureExtractor: `code/my_agents.py:444-521`
- DifficultyScorer: `code/my_agents.py:523-575`
- RouterAgent: `code/my_agents.py:578-878`

**模块最终目的**：
1. **生成压缩后的表格**：通过分析问题，提取需要的行列，压缩原始表格
2. **生成最终分数**：由两个核心分数（sem_score和cell_score）共同决定，用于路径选择

**两个核心分数**：

#### sem_score（语义难度分数）⭐

- **计算方式**：根据语义决定，通过LLM分析问题的语义复杂度
- **计算位置**：Router模块中的语义分析步骤
- **分数含义**：[0, 1]，0=简单语义，1=复杂语义
- **同时输出**：在计算sem_score时，会同时获取**哪些行列是需要用得上的**

#### cell_score（结构复杂度分数）⭐

- **计算方式**：根据使用到的表格大小决定
- **计算逻辑**：
  1. 在计算sem_score时，获取需要的行列信息（sel_rows, sel_cols）
  2. 用这些行列信息压缩表格，生成新的表格
  3. **cell_score = 压缩后表格大小 / 原表格大小**
- **分数含义**：[0, 1]，0=小范围数据，1=大范围数据
- **关键理解**：**cell_score就是压缩后表格的大小比例**

**模块内部组成**（代码实现结构）：

虽然模块2、3、4在逻辑上是一个整体，但在代码实现上分为三个子模块：

#### 2.1 FeatureExtractor（特征抽取子模块）

**代码位置**：`code/my_agents.py:444-521`

**职责**：
- 计算sem_score（语义难度分数）
- 同时获取需要的行列信息（sel_rows, sel_cols）

**核心方法**：`extract(question, table_schema)`

**输出**：
```python
(
    semantic_score: float,        # sem_score：语义难度分数 [0, 1]
    semantic_features: Dict,
    structural_hints: Dict,
    coarse_intent: str
)
```

**注意**：在实际代码中，行列信息的获取可能通过其他方式（如llm_cell_score），但在逻辑上，这些信息应该是在计算sem_score时一起获取的。

#### 2.2 表格压缩（压缩表格生成）

**代码位置**：`code/my_agents.py:755-822`（`RouterAgent._reduce_df()`）

**职责**：
- 使用需要的行列信息压缩表格
- 生成压缩后的表格（reduced_df）

**实现逻辑**：
1. 使用从语义分析获取的行列信息（sel_rows, sel_cols）
2. 过滤原始表格，只保留需要的行列
3. 生成压缩后的表格（reduced_df）

**cell_score计算**：
```python
# 压缩后表格大小
compressed_rows = len(reduced_df)
compressed_cols = len(reduced_df.columns)
compressed_size = compressed_rows × compressed_cols

# 原表格大小
original_rows = len(original_df)
original_cols = len(original_df.columns)
original_size = original_rows × original_cols

# cell_score = 压缩后表格大小 / 原表格大小
cell_score = compressed_size / original_size  # 范围 [0, 1]
```

**示例**：
- 原表格：1000行 × 10列 = 10,000单元格
- 压缩后：100行 × 3列 = 300单元格
- **cell_score = 300 / 10,000 = 0.03**

#### 2.3 DifficultyScorer（难度评分子模块）

**代码位置**：`code/my_agents.py:523-575`

**职责**：组合sem_score和cell_score，计算综合难度分数

**核心方法**：`score(sem_score, cell_score)`

**核心公式**：
```python
total_score = w_sem * sem_score + w_cell * cell_score
# 默认权重：w_sem=0.6, w_cell=0.4
```

**输出格式**：
```python
(
    difficulty_score: float,    # 综合难度分数 [0, 1]
    difficulty_level: str       # "easy" 或 "hard"
)
```

#### 2.4 RouterAgent（路径路由子模块）

**代码位置**：`code/my_agents.py:578-878`

**职责**：整合所有子模块，执行路由决策

**核心方法**：`route(state)`

**执行流程**：
1. 调用FeatureExtractor，获取sem_score和需要的行列信息
2. 使用行列信息压缩表格，计算cell_score
3. 调用DifficultyScorer，组合两个分数，得到综合难度分数
4. 根据综合难度分数和意图，决定路径类型（SIMPLE/COMPLEX）

**最终输出**：
- **压缩后的表格**：存储在 `state.df` 中
- **最终分数**：存储在 `state.difficulty_score` 和 `state.difficulty_level` 中
- **路径类型**：存储在 `state.route_type` 中

**Token消耗**：
- 语义分析（sem_score）：约1000 tokens
- 行列选择（如果需要单独调用）：约1200 tokens
- **总计：约2200 tokens**
- **但节省的后续token：约48,000 tokens（97%减少）**
- **净收益：约45,800 tokens节省**

---

### 3. SimplePathAgent（简单路径链式推理代理）

**文件位置**：`code/my_agents.py:1274-1343`

**职责**：
- 当 `route_type == "SIMPLE"` 时使用
- 1次LLM调用直接回答

**核心方法**：

#### `run_simple_path(question: str, table_schema: Dict[str, Any]) -> Dict[str, Any]`

**Token消耗**：
- 输入：约500 tokens（问题 + schema）
- 输出：约50 tokens（答案）
- **总计：约550 tokens**
- **对比MACT：减少98.9%的token节省**

---

### 4. PlannerAgent（复杂路径规划代理）

**文件位置**：`code/my_agents.py:928-998`

**职责**：
- 当 `route_type == "COMPLEX"` 时使用
- 生成分步执行计划（自然语言步骤 + Python代码）

**Token消耗**：
- 输入：约800 tokens（问题 + 压缩表格schema + prompt）
- 输出：约400 tokens（计划 + 代码）
- **总计：约1200 tokens**

---

### 5. Calculator（工具执行与计算模块）

**文件位置**：`code/my_agents.py:1004-1103`

**职责**：
- 安全执行Planner生成的Python代码
- 提取最终计算结果

**Token消耗**：
- **0 tokens**（纯代码执行，无LLM调用）

---

### 6. CriticAgent（轻量校验模块）

**文件位置**：`code/my_agents.py:1147-1225`

**职责**：
- 对执行结果进行基本校验
- 发现明显错误，要求REPLAN（如果需要）

**Token消耗**：
- 输入：约1200 tokens（问题 + plan + code + result + prompt）
- 输出：约100 tokens（verdict + feedback）
- **总计：约1300 tokens**

---

### 7. FinalAnswerAgent（最终答案生成模块）

**文件位置**：`code/my_agents.py:1349-1385`

**职责**：
- 格式化最终自然语言答案

**Token消耗**：
- 简单路径：约400 tokens
- 复杂路径：约600 tokens
- **平均：约500 tokens**

---

### 8. TableQAPipeline（流程编排器）

**文件位置**：`code/my_agents.py:1391-1509`

**职责**：
- 串联所有模块，提供统一入口
- 管理状态流转和重规划循环

---

## 数据流与状态管理

### TQASessionState（会话状态对象）

**核心字段**：

```python
# 输入数据
question: str                      # 用户问题
df: pd.DataFrame                  # 当前表格（Router模块压缩后）
table_schema: Dict                 # 表格schema

# Router模块输出
route_type: Optional[str]          # "SIMPLE" 或 "COMPLEX"
sem_score: float                   # ⭐ 语义难度分数 [0, 1]
cell_score: float                  # ⭐ 结构复杂度分数 [0, 1]
difficulty_score: Optional[float]  # 综合难度分数 [0, 1]
difficulty_level: Optional[str]    # "easy" 或 "hard"
coarse_intent: Optional[str]       # 粗意图标签
routing_context: Dict              # 路由上下文（包含两个核心分数）

# 规划和执行信息
plan_steps: List[str]              # 规划步骤列表
code_str: str                      # 可执行代码
exec_success: bool                 # 执行是否成功
final_value: Any                   # 最终数值结果

# 最终答案
final_answer: Optional[str]        # 最终自然语言答案
```

---

## Token消耗优化策略

### 优化策略总结

| 优化策略 | 实现位置 | 优化效果 |
|---------|---------|---------|
| **Schema构建** | InputHandler | 使用schema（500 tokens）而非完整表格（50,000 tokens），减少99% |
| **表格压缩** | Router模块 | 压缩表格（1,500 tokens）替代完整表格（50,000 tokens），减少97% |
| **路径选择** | Router模块 | 简单问题走SimplePath（550 tokens）而非复杂路径（48,500 tokens），减少98.9% |

### Token消耗对比

**MACT基准（复杂问题）**：~48,500 tokens

**本项目（假设70%简单，30%复杂）**：
- 简单问题（70%）：Router(2,200) + SimplePath(550) + FinalAnswer(400) = 3,150 tokens
- 复杂问题（30%）：Router(2,200) + Planner(1,200) + Calculator(0) + Critic(1,300) + FinalAnswer(600) = 5,300 tokens
- **平均：0.7 × 3,150 + 0.3 × 5,300 = 3,855 tokens**

**优化效果**：
- **减少比例：约92.0%的token节省**

---

## 关键设计决策

### 1. 为什么Router模块要同时输出压缩表格和最终分数？

**原因**：
- 压缩表格：减少后续模块的token消耗（97%减少）
- 最终分数：决定走简单路径还是复杂路径（简单问题减少98.9%）

**权衡**：
- Router模块需要额外的LLM调用来分析问题和选择行列（~2,200 tokens）
- 但后续每个模块都节省了大量token
- **净收益：约45,800 tokens节省**

### 2. 为什么cell_score = 压缩后表格大小 / 原表格大小？

**原因**：
- cell_score衡量问题需要的数据范围
- 压缩后的表格大小直接反映了需要的数据范围
- 简单直观，易于理解和调试

**优势**：
- 计算简单，无需额外的LLM调用
- 准确反映数据范围需求
- 与表格压缩过程紧密结合

---

**文档维护**：本文档会在每次代码优化后同步更新，确保反映最新的实现逻辑。

