# 目前工程实现文档

> 本文档实时记录表格问答系统各模块的当前实现逻辑和设计细节。
> 每次代码优化后，本文档会同步更新。

最后更新时间：2025年1月（代码优化版本）

---

## 目录

1. [项目概述](#项目概述)
2. [整体架构](#整体架构)
3. [核心分数系统（sem_score & cell_score）](#核心分数系统sem_score--cell_score) ⭐
4. [核心模块详解](#核心模块详解)
5. [数据流与状态管理](#数据流与状态管理)
6. [Token消耗优化策略](#token消耗优化策略)
7. [关键设计决策](#关键设计决策)

---

## 项目概述

### 项目目标

本项目实现了一个**任务特征感知的链式推理路径选择优化方法**，核心目标是：
- **提高表格数据获取的准确性**
- **大幅减少LLM调用的token消耗**（目标：减少95%+）

### 核心创新点

1. **自适应路径选择**：根据问题难度和特征，智能选择简单路径或复杂路径
2. **表格压缩优化**：在路由阶段压缩表格，只保留需要的数据
3. **轻量校验机制**：对复杂路径结果进行基本校验，避免过度消耗

---

## 整体架构

### 系统架构图

```
用户输入 (Q, T)
    ↓
[InputHandler] 输入管理与格式统一
    ↓
[Router] 路径路由模块
    ├─→ [FeatureExtractor] 特征抽取
    ├─→ [DifficultyScorer] 难度评分
    └─→ 路径决策 (SIMPLE/COMPLEX)
    ↓
    ├─→ SIMPLE路径
    │       ↓
    │   [SimplePathAgent] 简单路径推理
    │       ↓
    │   [FinalAnswerAgent] 生成最终答案
    │
    └─→ COMPLEX路径
            ↓
        [Planner] 复杂路径规划
            ↓
        [Calculator] 代码执行
            ↓
        [LightCritic] 轻量校验
            ↓ (如果REPLAN，返回Planner)
        [FinalAnswerAgent] 生成最终答案
```

### 模块列表

| 模块名称 | 文件位置 | 职责 | Token消耗优化作用 |
|---------|---------|------|------------------|
| InputHandler | `my_agents.py:126` | 输入预处理和格式统一 | 构建轻量schema，减少后续token |
| Router模块（包含FeatureExtractor/DifficultyScorer/RouterAgent） | `my_agents.py:444-878` | **核心模块**：生成压缩表格+最终分数 | **核心优化：表格压缩97%+，路径选择减少98.9%** |
| SimplePathAgent | `my_agents.py:1274` | 简单路径推理 | **核心优化：简单问题减少98.9%** |
| PlannerAgent | `my_agents.py:928` | 复杂路径规划 | 使用压缩表格 |
| Calculator | `my_agents.py:1004` | 代码执行 | 无token消耗 |
| CriticAgent | `my_agents.py:1147` | 轻量校验 | 使用压缩表格 |
| FinalAnswerAgent | `my_agents.py:1349` | 最终答案生成 | 轻量格式化 |
| TableQAPipeline | `my_agents.py:1391` | 流程编排器 | 统一入口和状态管理 |

---

## 核心模块详解

### 1. InputHandler（输入管理模块）

**文件位置**：`code/my_agents.py:126-224`

**职责**：
- 接收原始输入：自然语言问题 Q，以及表格 T（CSV / JSON / pandas.DataFrame）
- 负责基本预处理、格式统一
- 构建轻量级的表格schema（而非完整表格）

**核心方法**：

#### `process_input(question: str, table: Any) -> Tuple[str, pd.DataFrame, Dict[str, Any]]`

**实现逻辑**：
1. 规范化问题文本（去除首尾空白）
2. 将表格统一转换为 pandas.DataFrame
3. 构建表格schema（包含列名、预览文本、行列数等）

**关键优化**：
- **Schema构建策略**：动态调整预览行数（默认5行，小表全部预览）
- **Token节省**：从完整表格（可能50,000+ tokens，1000行×10列）减少到schema（约500 tokens，5行预览+列名）
- **减少比例**：约99%的token节省
- **代码位置**：`build_table_schema()` 方法实现动态预览行数逻辑

**输出格式**：
```python
(
    normalized_question: str,  # 规范化的问题文本
    df: pd.DataFrame,          # 统一的表格数据
    table_schema: Dict {       # 轻量schema
        "columns": List[str],      # 列名列表
        "preview_text": str,       # 前5行的文本预览
        "num_rows": int,           # 总行数
        "num_cols": int            # 总列数
    }
)
```

**Token消耗**：
- 输入处理：纯内存操作，0 tokens
- Schema构建：纯内存操作，0 tokens
- **后续模块使用schema：约500 tokens（相比完整表格节省99%）**

---

### 2. FeatureExtractor（任务特征抽取模块）⭐

**文件位置**：`code/my_agents.py:444-521`

**职责**：
- **核心功能**：计算 **sem_score（语义难度分数）** ⭐
- 从问题和表格schema中提取语义特征和结构特征
- 识别粗粒度意图标签

**核心方法**：

#### `extract(question: str, table_schema: Dict[str, Any]) -> Tuple[float, Dict, Dict, str]`

**实现逻辑**：
1. 构建prompt：包含问题、列名、表格预览（使用schema，而非完整表格）
2. 调用LLM提取特征（JSON格式）
3. 解析JSON并验证语义分数范围 [0, 1]
4. 如果LLM输出不完整，使用规则兜底

**核心输出**：**sem_score（语义难度分数）** ⭐
- 这是路径选择的核心依据之一
- 通过LLM分析问题的语义复杂度得到
- 范围 [0, 1]，0=简单语义，1=复杂语义

**输出格式**：
```python
(
    semantic_score: float,           # ⭐ 语义难度分数 [0, 1] - 核心输出
    semantic_features: Dict {        # 语义特征
        "has_aggregation_op": bool,
        "has_comparison_op": bool,
        "has_temporal_pattern": bool,
        "num_numeric_tokens": int,
        "num_constraints": int,
        "mentions_ratio_or_rate": bool
    },
    structural_hints: Dict {         # 结构提示
        "requires_cross_row_aggregation": bool,
        "requires_cross_col_aggregation": bool,
        "multi_table_or_multi_segment": bool
    },
    coarse_intent: str               # 粗意图标签: lookup/aggregation/comparison/ranking/trend/multi_step
)
```

**Token消耗**：
- Prompt构建：约800 tokens（问题 + schema）
- LLM调用：输入800 tokens，输出约200 tokens
- **总计：约1000 tokens**

**优化点**：
- ✅ 使用schema而非完整表格（节省99%的token）
- ✅ 一次性提取所有特征（而非多次调用）

---

### 3. DifficultyScorer（难度评分模块）

**文件位置**：`code/my_agents.py:523-575`

**职责**：
- 结合语义分数和结构复杂度分数，计算综合难度分数
- 划分难度等级（easy/hard）

**核心方法**：

#### `score(sem_score: float, cell_score: float) -> Tuple[float, str]`

**实现逻辑**：
1. 加权线性组合：`total_score = w_sem * sem_score + w_cell * cell_score`
2. 约束到 [0, 1] 范围
3. 根据阈值划分难度等级：
   - `total_score < threshold (0.5)` → "easy"
   - `total_score >= threshold` → "hard"

**参数配置**：
- `w_sem = 0.6`：语义分数权重
- `w_cell = 0.4`：结构复杂度分数权重
- `threshold = 0.5`：难度划分阈值

**输出格式**：
```python
(
    difficulty_score: float,    # 综合难度分数 [0, 1]
    difficulty_level: str       # 难度等级: "easy" 或 "hard"
)
```

**Token消耗**：
- **0 tokens**（纯计算，无LLM调用）

---

### 4. RouterAgent（路径路由模块）⭐

**文件位置**：`code/my_agents.py:578-878`

**职责**：
- **核心功能1**：计算 **cell_score（结构复杂度分数）** ⭐
- **核心功能2**：组合 **sem_score + cell_score**，计算综合难度分数 ⭐
- **核心优化模块1**：表格压缩（减少97%+的后续token消耗）
- **核心优化模块2**：路径选择（简单问题走简单路径）

**核心方法**：

#### `route(state: TQASessionState) -> TQASessionState`

**实现逻辑**（4个步骤）：

**步骤1：计算cell_score（结构复杂度分数）⭐ + 表格压缩**
```python
# 1.1 ⭐ 计算cell_score：LLM分析问题，选择需要的行和列
# 返回值：cell_score [0, 1] - 结构复杂度分数（核心输出）
cell_score, estimated_cells, sel_rows, sel_cols = self.llm_cell_score(
    question=q, row_names=row_names, col_names=col_names, df=state.df
)

# 1.2 压缩表格（如果可能）
reduced_df, reduce_info = self._reduce_df(state.df, sel_rows, sel_cols)
if reduce_info["reduced"]:
    state.df_full = state.df           # 保存原表（用于调试）
    state.df = reduced_df              # 使用压缩表
    state.table_schema = _build_table_schema(reduced_df)  # 更新schema
    state.table_reduced = True
```

**步骤2：计算sem_score（语义难度分数）⭐ + 粗意图提取**
```python
# ⭐ 计算sem_score：通过LLM分析问题的语义复杂度
# 返回值：sem_score [0, 1] - 语义难度分数（核心输出）
sem_score, semantic_features, structural_hints, coarse_intent = \
    self.feature_extractor.extract(q, state.table_schema)
```

**步骤3：组合sem_score和cell_score，计算综合难度分数⭐**
```python
# ⭐ 核心步骤：组合两个核心分数
# total_score = 0.6 × sem_score + 0.4 × cell_score
total_score, difficulty_level = self._score_difficulty(sem_score, cell_score)
```

**步骤4：路径决策**
```python
complex_intents = {"aggregation", "comparison", "ranking", "trend", "multi_step"}
if coarse_intent in complex_intents:
    state.route_type = "COMPLEX"  # 复杂意图 → 复杂路径
elif difficulty_level == "easy":
    state.route_type = "SIMPLE"   # 难度低 → 简单路径
elif difficulty_level == "hard":
    state.route_type = "COMPLEX"  # 难度高 → 复杂路径
else:
    state.route_type = self._rule_based_route(q) or "COMPLEX"  # 规则兜底
```

**关键优化：表格压缩（核心Token优化步骤）**

**优化效果**：
- 原始表格：1000行 × 10列 = 10,000单元格 ≈ 50,000 tokens
- 压缩后：100行 × 3列 = 300单元格 ≈ 1,500 tokens
- **减少比例：97%的token节省**

**实现位置**：
- `_reduce_df()` 方法：基于LLM选择的行列压缩表格
- 代码位置：`my_agents.py:755-795`
- 所有后续模块（Planner、Calculator、Critic）都使用压缩后的表格（`state.df`）

**压缩策略**：
1. LLM分析问题，选择相关的列名（`sel_cols`）
2. LLM分析问题，选择相关的行标识（`sel_rows`）
3. 过滤表格：只保留选中的列和行
4. 后续所有模块（Planner、Calculator、Critic）都使用压缩后的表格

**Token消耗**：
- `llm_cell_score`：约1200 tokens（选择行列）
- `feature_extractor.extract`：约1000 tokens（特征提取）
- **总计：约2200 tokens**
- **但节省的后续token：约48,000 tokens（97%减少，每个模块节省约48,000 tokens）**
- **净收益：约45,800 tokens节省**

**代码实现细节**：
- `_reduce_df()` 方法实现表格压缩逻辑
- 压缩后的表格存储在 `state.df`，原始表格保存在 `state.df_full`（用于调试）
- 压缩信息记录在 `state.routing_context` 中

---

### 3. SimplePathAgent（简单路径链式推理代理）

**文件位置**：`code/my_agents.py:1274-1343`

**职责**：
- **核心优化模块**：处理简单问题，1次LLM调用直接回答
- 当 `route_type == "SIMPLE"` 时使用

**核心方法**：

#### `run_simple_path(question: str, table_schema: Dict[str, Any]) -> Dict[str, Any]`

**实现逻辑**：
1. 构建简单prompt：问题 + 表格预览（使用schema）
2. 调用LLM直接生成答案和简要推理
3. 不进行显式Planner/Tool规划
4. 不进行多步推理

**Prompt示例**：
```
你是一个政务统计表格问答助手。

【问题】
2021年的营收是多少？

【表格预览】
Year | Revenue | Profit
2020 | 150     | 30
2021 | 180     | 35

请根据表格内容，用简洁的自然语言回答问题。
如果需要进行简单计算（如差值、比较），请直接计算并给出结果。
回答格式：先用1-2句话说明推理过程，然后给出最终答案。
```

**输出格式**：
```python
{
    "answer": str,              # 最终答案
    "reasoning_trace": str,     # 推理轨迹（完整响应）
    "meta": {
        "path": "simple",
        "tool_calls": 0
    }
}
```

**Token消耗**：
- 输入：约500 tokens（问题 + schema）
- 输出：约50 tokens（答案）
- **总计：约550 tokens**

**对比MACT**：
- MACT（复杂路径）：约48,500 tokens
- SimplePathAgent：约550 tokens
- **减少比例：98.9%的token节省**

---

### 4. PlannerAgent（复杂路径规划代理）

**文件位置**：`code/my_agents.py:928-998`

**职责**：
- 当 `route_type == "COMPLEX"` 时使用
- 生成分步执行计划（自然语言步骤 + Python代码）

**核心方法**：

#### `plan(state: TQASessionState) -> TQASessionState`

**实现逻辑**：
1. 构建prompt：问题 + 表格schema（使用压缩后的表格） + Critic反馈（如果有）
2. 调用LLM生成计划（包含[PLAN]和[CODE]两部分）
3. 解析LLM输出，提取plan_steps和code_str

**Prompt结构**：
```
You are a table reasoning planner and programmer.

Question: {question}
Table Schema (first few rows): {table_preview}
Columns: {col_names}

If there was a previous critic feedback, consider it:
{critic_feedback}

[PLAN]
Step1: ...
Step2: ...

[CODE]
# your python code here
final_answer_value = ...
```

**输出格式**（更新state）：
```python
state.planner_raw_output: str      # LLM原始输出
state.plan_steps: List[str]        # 解析后的步骤列表，如["Step1: ...", "Step2: ..."]
state.code_str: str                # 可执行的Python代码
```

**Token消耗**：
- 输入：约800 tokens（问题 + 压缩表格schema + prompt）
- 输出：约400 tokens（计划 + 代码）
- **总计：约1200 tokens**

**优化点**：
- ✅ 使用压缩后的表格schema（而非完整表格）
- ✅ 一次性生成计划和代码（而非分步生成）
- ✅ 如果后续Critic要求REPLAN，可以基于反馈重新规划

---

### 5. Calculator（工具执行与计算模块）

**文件位置**：`code/my_agents.py:1004-1103`

**职责**：
- 安全执行Planner生成的Python代码
- 提取最终计算结果

**核心方法**：

#### `execute(state: TQASessionState) -> TQASessionState`

**实现逻辑**：
1. 在受限环境中执行代码（限制builtins，防止副作用）
2. 从执行结果中提取 `final_answer_value`
3. 处理执行错误

**安全执行环境**：
```python
allowed_builtins = {
    "abs", "min", "max", "sum", "len", "range", "round", "sorted"
}
global_env = {"__builtins__": allowed_builtins, "pd": pd}
local_env = {"df": state.df}  # 传入压缩后的表格
exec(code_str, global_env, local_env)
final_value = local_env.get("final_answer_value")
```

**输出格式**（更新state）：
```python
state.exec_success: bool           # 执行是否成功
state.exec_error: Optional[str]    # 错误信息（如果有）
state.exec_locals: Dict            # 执行后的局部变量
state.final_value: Any             # 最终数值结果
```

**Token消耗**：
- **0 tokens**（纯代码执行，无LLM调用）

---

### 8. CriticAgent（轻量校验模块）

**文件位置**：`code/my_agents.py:1147-1225`

**职责**：
- 对执行结果进行基本校验
- 发现明显错误，要求REPLAN（如果需要）

**核心方法**：

#### `review(state: TQASessionState) -> TQASessionState`

**实现逻辑**：
1. 保护措施：如果执行失败或代码为空，直接REPLAN
2. 否则，调用LLM评审plan/code/result的正确性
3. 解析LLM输出，提取verdict（PASS/REPLAN）和feedback

**Prompt结构**：
```
You are a careful auditor for table-based numerical reasoning.

[QUESTION] {question}
[PLAN] {plan_text}
[CODE] {code_str}
[RESULT]
success = {exec_success}
final_answer_value = {final_value}
error = {exec_error}

[VERDICT] PASS or REPLAN
[COMMENT] ...
[HINT_FOR_PLANNER] ...
```

**输出格式**（更新state）：
```python
state.critic_verdict: str          # "PASS" 或 "REPLAN"
state.critic_feedback: str         # 反馈信息（供Planner参考）
state.critic_raw_output: str       # LLM原始输出
```

**Token消耗**：
- 输入：约1200 tokens（问题 + plan + code + result + prompt）
- 输出：约100 tokens（verdict + feedback）
- **总计：约1300 tokens**

**优化点**：
- ✅ 使用压缩后的表格信息（通过plan和code间接使用）
- ✅ 轻量校验（不做多智能体辩论，那是点2的功能）

**REPLAN机制**：
- 如果 `critic_verdict == "REPLAN"`，TableQAPipeline会重新调用Planner
- Planner会收到 `critic_feedback`，基于反馈重新规划
- 最多允许 `max_replan` 次（默认2次）

---

### 7. FinalAnswerAgent（最终答案生成模块）

**文件位置**：`code/my_agents.py:1349-1385`

**职责**：
- 格式化最终自然语言答案

**核心方法**：

#### `respond(state: TQASessionState) -> TQASessionState`

**实现逻辑**：

**简单路径**：
- 使用表格预览 + 问题，直接生成答案

**复杂路径**：
- 使用问题 + 规划步骤 + 最终计算结果，生成正式答案

**输出格式**（更新state）：
```python
state.final_answer: str            # 最终自然语言答案
```

**Token消耗**：
- 简单路径：约400 tokens
- 复杂路径：约600 tokens
- **平均：约500 tokens**

---

### 8. TableQAPipeline（流程编排器）

**文件位置**：`code/my_agents.py:1391-1509`

**职责**：
- 串联所有模块，提供统一入口
- 管理状态流转和重规划循环

**核心方法**：

#### `run(state: TQASessionState) -> TQASessionState`

**实现逻辑**（完整流程）：

```
1. Router.route(state)
   ↓
2. 如果 state.route_type == "SIMPLE":
   ├─→ SimplePathAgent.run_simple_path()
   └─→ FinalAnswerAgent.respond()  [可选，如果SimplePathAgent已生成答案]
   
   否则 state.route_type == "COMPLEX":
   ├─→ 循环（最多max_replan+1次）:
   │   ├─→ PlannerAgent.plan()
   │   ├─→ Calculator.execute()
   │   ├─→ CriticAgent.review()
   │   └─→ 如果critic_verdict == "PASS": 退出循环
   └─→ FinalAnswerAgent.respond()
```

**Token消耗总结**：

| 路径类型 | 各模块消耗 | 总计 |
|---------|-----------|------|
| 简单路径 | Router(2200) + SimplePath(550) + FinalAnswer(400) | ~3,150 tokens |
| 复杂路径（1次规划） | Router(2200) + Planner(1200) + Calculator(0) + Critic(1300) + FinalAnswer(600) | ~5,300 tokens |
| 复杂路径（2次规划） | Router(2200) + [Planner(1200) + Calculator(0) + Critic(1300)]×2 + FinalAnswer(600) | ~7,600 tokens |

**对比MACT**：
- MACT平均：~48,500 tokens（复杂问题，5步ReAct循环）
- 本项目平均：~3,150 tokens（假设70%简单，30%复杂，平均约4,500 tokens）
- **减少比例：约90.7%的token节省**

---

## 数据流与状态管理

### TQASessionState（会话状态对象）

**文件位置**：`code/my_agents.py:76-124`

**作用**：作为所有模块之间的状态传递载体

**核心字段**：

```python
# 输入数据
question: str                      # 用户问题
df: pd.DataFrame                  # 当前表格（可能被Router压缩）
table_schema: Dict                 # 表格schema

# 路由信息
route_type: Optional[str]          # "SIMPLE" 或 "COMPLEX"
semantic_features: Dict            # 语义特征
structural_features: Dict          # 结构特征
difficulty_score: Optional[float]  # 难度分数 [0, 1]
difficulty_level: Optional[str]    # "easy" 或 "hard"
coarse_intent: Optional[str]       # 粗意图标签
routing_context: Dict              # 路由上下文

# 规划信息
planner_raw_output: str            # Planner原始输出
plan_steps: List[str]              # 规划步骤列表
code_str: str                      # 可执行代码

# 执行信息
exec_success: bool                 # 执行是否成功
exec_error: Optional[str]          # 错误信息
final_value: Any                   # 最终数值结果

# 校验信息
critic_verdict: Optional[str]      # "PASS" 或 "REPLAN"
critic_feedback: str               # 校验反馈

# 最终答案
final_answer: Optional[str]        # 最终自然语言答案
```

**状态流转**：

```
初始化 state (question, df, table_schema)
    ↓
Router.route(state) → 更新 route_type, routing_context, 压缩df
    ↓
[分支1: SIMPLE]
SimplePathAgent.run_simple_path() → 生成answer
    ↓
FinalAnswerAgent.respond() → 更新final_answer
    ↓
返回state

[分支2: COMPLEX]
Planner.plan(state) → 更新plan_steps, code_str
    ↓
Calculator.execute(state) → 更新exec_success, final_value
    ↓
Critic.review(state) → 更新critic_verdict, critic_feedback
    ↓
如果critic_verdict == "REPLAN": 返回Planner
否则: FinalAnswerAgent.respond() → 更新final_answer
    ↓
返回state
```

---

## Token消耗优化策略

### 优化策略总结

| 优化策略 | 实现位置 | 优化效果 |
|---------|---------|---------|
| **Schema构建** | InputHandler | 使用schema（500 tokens）而非完整表格（50,000 tokens），减少99% |
| **表格压缩** | RouterAgent._reduce_df | 压缩表格（1,500 tokens）替代完整表格（50,000 tokens），减少97% |
| **路径选择** | RouterAgent.route | 简单问题走SimplePath（550 tokens）而非复杂路径（48,500 tokens），减少98.9% |
| **压缩表格传递** | 所有后续模块 | Planner/Critic使用压缩表格，而非完整表格 |
| **简化采样** | PlannerAgent | 单次生成（1,200 tokens）而非多次采样（6,000 tokens），减少80% |

### Token消耗对比

**MACT基准（复杂问题）**：
- ReAct循环（5步）：~27,000 tokens
- 工具调用（3次）：~16,500 tokens
- 采样（5倍）：~5,000 tokens
- **总计：~48,500 tokens**

**本项目（假设70%简单，30%复杂）**：
- 简单问题（70%）：Router(2,200) + SimplePath(550) + FinalAnswer(400) = 3,150 tokens
- 复杂问题（30%）：Router(2,200) + Planner(1,200) + Calculator(0) + Critic(1,300) + FinalAnswer(600) = 5,300 tokens
- **平均：0.7 × 3,150 + 0.3 × 5,300 = 3,855 tokens**

**优化效果**：
- **减少比例：约92.0%的token节省**
- **简单问题：减少98.9%**
- **复杂问题：减少89.1%**

---

## 关键设计决策

### 1. 为什么在Router阶段压缩表格？

**原因**：
- Router阶段已经分析了问题，知道需要哪些行和列
- 后续所有模块（Planner、Calculator、Critic）都可以使用压缩表格
- 一次压缩，全程受益

**权衡**：
- Router阶段需要额外的LLM调用来选择行列（~1,200 tokens）
- 但后续每个模块都节省了大量token（每个模块节省~48,000 tokens）
- **净收益：约45,800 tokens节省**

### 2. 为什么使用Schema而非完整表格？

**原因**：
- Schema（列名+预览）足以让LLM理解表格结构
- 完整表格传入LLM会消耗大量token（可能50,000+ tokens）
- Schema只需要约500 tokens，减少99%

**权衡**：
- 如果问题需要查看表格的细节数据，Schema可能不够
- 但Router已经选择了相关行列，压缩后的表格足以处理问题
- **总体收益：大幅减少token消耗**

### 3. 为什么简单路径只调用1次LLM？

**原因**：
- 简单问题（如查值、直接比较）不需要多步推理
- 1次LLM调用足以生成答案
- 多步推理会增加token消耗，但不提高准确性

**权衡**：
- 如果问题被误判为简单问题，可能无法正确回答
- 但Router的路径选择准确率通常较高（通过特征提取和难度评分）
- **总体收益：简单问题减少98.9%的token消耗**

### 4. 为什么复杂路径允许REPLAN？

**原因**：
- 复杂问题可能需要多次尝试才能找到正确答案
- Critic可以发现明显错误，要求重新规划
- 最多允许2次REPLAN，避免无限循环

**权衡**：
- REPLAN会增加token消耗（每次REPLAN约2,500 tokens）
- 但可以提高复杂问题的准确性
- **总体收益：在准确性和token消耗之间取得平衡**

---

## 日志系统

### 日志模块

**文件位置**：`code/logger.py`

**功能**：
- 记录每个模块的执行逻辑和关键信息
- 支持分级日志（DEBUG/INFO/WARNING/ERROR）
- 可以输出到控制台或文件

**日志记录内容**：
- InputHandler：输入处理信息
- FeatureExtractor：特征提取结果
- DifficultyScorer：难度评分结果
- Router：路由决策和表格压缩信息
- SimplePathAgent：简单路径推理结果
- Planner：规划步骤和代码
- Calculator：执行状态和结果
- Critic：校验结果和反馈
- Orchestrator：完整流程信息

---

## 后续优化方向

### 1. 进一步优化（点2的功能）

- 多智能体辩论
- 多路径交叉验证
- 证据聚合

### 2. 性能优化

- 缓存压缩结果
- 并行处理多个问题
- 优化LLM调用（批量处理）

### 3. 准确性提升

- 改进路径选择准确率
- 改进表格压缩策略
- 改进Critic校验逻辑

---

**文档维护**：本文档会在每次代码优化后同步更新，确保反映最新的实现逻辑。

