# 目前工程实现文档（智能体0226）

> 本文档根据 `code/my_agents.py` 的当前实现整理，描述实际落地的模块、流程与分数逻辑。

最后更新时间：2026-02-26

---

## 目录

1. [项目概述](#项目概述)
2. [整体架构](#整体架构)
3. [核心分数系统（sem_score & cell_score）](#核心分数系统sem_score--cell_score) ⭐
4. [核心模块详解](#核心模块详解)
5. [数据流与状态管理](#数据流与状态管理)
6. [Token消耗优化策略](#token消耗优化策略)
7. [运行依赖与注意事项](#运行依赖与注意事项)

---

## 项目概述

### 项目目标

当前实现提供一个简化版表格问答管线：
- 通过路由器判定 SIMPLE/COMPLEX 路径
- 复杂路径生成计划 + 代码并执行
- 轻量校验后生成最终答案
- SIMPLE 路径单次 LLM 直接回答

### 当前实现的核心特点

1. **语义分数 + 结构分数的路由决策**：`sem_score` + `cell_score` 决定路径
2. **表格压缩**：Router 基于列选择与行过滤压缩表格
3. **规划-执行-校验闭环**：Planner → Calculator → Critic
4. **统一状态对象**：`TQASessionState` 贯穿全流程

---

## 整体架构

```
用户输入 (Q, T)
    ↓
[InputHandler] 输入管理与 schema 构建
    ↓
[RouterAgent] sem_score + 压缩表格 + cell_score → 路由
    ↓
    ├─→ SIMPLE 路径
    │       ↓
    │   [SimplePathAgent] 直接回答
    │
    └─→ COMPLEX 路径
            ↓
        [PlannerAgent] 生成计划 + Python 代码
            ↓
        [Calculator] 执行代码
            ↓
        [CriticAgent] 轻量校验（可 REPLAN）
            ↓
        [FinalAnswerAgent] 生成最终答案
```

### 模块列表

| 模块名称 | 文件位置 | 职责 | 备注 |
|---------|---------|------|------|
| InputHandler | `code/my_agents.py` | 输入预处理 + schema 构建 | 轻量 schema |
| FeatureExtractor | `code/my_agents.py` | 语义特征抽取 | LLM JSON 输出 |
| DifficultyScorer | `code/my_agents.py` | 综合评分 | 线性加权 |
| RouterAgent | `code/my_agents.py` | 表格压缩 + 路由决策 | 核心模块 |
| SimplePathAgent | `code/my_agents.py` | 简单路径 LLM 直接回答 | 1 次调用 |
| PlannerAgent | `code/my_agents.py` | 计划 + 代码生成 | LLM 输出 `[PLAN]/[CODE]` |
| Calculator | `code/my_agents.py` | 代码执行 | 受限内置函数 |
| CriticAgent | `code/my_agents.py` | 轻量校验 | PASS/REPLAN |
| FinalAnswerAgent | `code/my_agents.py` | 最终答案生成 | COMPLEX 用 |
| TableQAPipeline | `code/my_agents.py` | 流程编排 | 统一入口 |

---

## 核心分数系统（sem_score & cell_score）⭐

### 1. sem_score（语义难度分数）

**定义**：基于问题语义复杂度的评分。

**实现方式**：
- LLM 输出 JSON，包含 `sem_score` 与语义 flags
- 范围 `[0, 1]`

**位置**：`FeatureExtractor.extract()`

---

### 2. cell_score（结构复杂度分数）

**定义**：压缩后表格大小与原表格大小的比例。

**实现方式**：
1. LLM 预测需要的列与行过滤条件
2. Router 压缩表格
3. 计算：
```python
cell_score = (reduced_rows * reduced_cols) / (original_rows * original_cols)
```

**位置**：`RouterAgent._reduce_df()` + `RouterAgent._compute_cell_score()`

---

### 3. 分数组合：综合难度计算

**公式**：
```python
total_score = 0.6 * sem_score + 0.4 * cell_score
```

**难度划分**：
```python
if total_score < 0.5:
    difficulty_level = "easy"   # SIMPLE
else:
    difficulty_level = "hard"   # COMPLEX
```

**位置**：`DifficultyScorer.score()`

---

## 核心模块详解

### 1. InputHandler（输入管理）

**方法**：`process_input(question, table)`

**职责**：
- 规范化问题
- 表格统一为 DataFrame
- 构建 schema：`columns / preview_text / num_rows / num_cols / row_names_str / col_names_str`

---

### 2. FeatureExtractor（语义特征抽取）

**方法**：`extract(question, table_schema)`

**输出**：
- `sem_score`
- `coarse_intent`
- `selected_columns`
- `row_filter`
- `semantic_flags`

---

### 3. RouterAgent（路由 + 压缩）

**流程**：
1. 调用 FeatureExtractor
2. `_reduce_df()` 根据 `selected_columns` 与 `row_filter` 裁剪
3. 计算 `cell_score`
4. DifficultyScorer 给出综合难度
5. 路由 SIMPLE / COMPLEX

**压缩逻辑**：
- 列裁剪：保留 LLM 预测的列
- 行裁剪：支持 `{ "column": "列名", "values": [...] }`

---

### 4. SimplePathAgent（简单路径）

**方法**：`run_simple_path(state)`

**行为**：
- 直接基于问题 + 表格预览回答

---

### 5. PlannerAgent / Calculator / CriticAgent

- Planner 输出 `[PLAN]/[CODE]`
- Calculator 执行代码（限定内置函数）
- Critic 输出 PASS/REPLAN，并给出反馈

---

### 6. FinalAnswerAgent

- SIMPLE 路径默认不使用（由 SimplePathAgent 接管）
- COMPLEX 路径基于 `plan_steps + final_value` 生成正式回答

---

### 7. TableQAPipeline（编排器）

**方法**：
- `build_state_from_table()`
- `run()`

**REPLAN**：
- `max_replan` 默认 2

---

## 数据流与状态管理

### TQASessionState（当前字段）

```python
# 输入数据
question: str
df: pd.DataFrame
table_schema: Dict

# Router 输出
route_type: Optional[str]
sem_score: Optional[float]
cell_score: Optional[float]
coarse_intent: Optional[str]
selected_columns: List[str]
row_filter: Dict[str, Any]
table_reduced: bool
original_shape: Optional[Tuple[int, int]]
reduced_shape: Optional[Tuple[int, int]]
semantic_features: Dict
structural_features: Dict
difficulty_score: Optional[float]
difficulty_level: Optional[str]
routing_context: Dict

# Planner
planner_raw_output: str
plan_steps: List[str]
code_str: str

# Calculator
exec_success: bool
exec_error: Optional[str]
exec_locals: Dict[str, Any]
final_value: Any

# Critic
critic_raw_output: str
critic_verdict: Optional[str]
critic_feedback: str

# Final answer
final_answer: Optional[str]
simple_answer: Optional[str]
```

---

## Token消耗优化策略

当前实现的主要优化：
- **表格压缩**：仅保留预测相关行列
- **轻量 schema**：Planner 与 SimplePath 仅使用预览与列名
- **简单路径直答**：SIMPLE 路径避免 Planner/Calculator/Critic

---

## 运行依赖与注意事项

**依赖**：
- Python 3.x
- `pandas`
- `utils.table2df`（本仓库 `code/utils.py`）
- 需注入 `llm_fn`（函数签名 `Callable[[str], str]`）

**潜在运行风险**：
- `FeatureExtractor` 依赖 LLM 输出严格 JSON，失败时回退为规则
- `Calculator` 内置函数受限，复杂代码可能执行失败
- 表格压缩使用 LLM 预测，准确性依赖模型

---

**文档维护**：本文档随 `code/my_agents.py` 更新而更新。
