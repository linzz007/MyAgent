# 语义特征参数获取方式说明

> 本文档详细说明 `semantic_flags` 中的各个参数（has_aggregation, has_comparison等）是如何获取的。
> 这是 Router 模块"特征提取"步骤的核心实现细节。

最后更新时间：2026年1月7日

---

## 核心结论

**这些语义特征参数是通过 LLM（大语言模型）分析问题文本得到的，不是代码自动提取的。**

具体流程：
1. **输入**：问题文本 + 表格列名
2. **LLM 分析**：LLM 根据 prompt 要求，分析问题的语义特征
3. **输出**：LLM 返回 JSON 格式的 `semantic_flags`
4. **解析**：代码解析 JSON，提取各个布尔/整数特征
5. **计算**：使用公式 `compute_sem_score()` 计算最终的 `sem_score`

---

## 详细流程

### 步骤 1：构建 Prompt

**代码位置**：`code/my_agents.py:724-727`

```python
# 构建 prompt：只提供问题和列名，减少 token 消耗
col_names = ", ".join(str(c) for c in table_schema.get("columns", []))  # 列名字符串
prompt = self.prompt_tmpl.format(
    question=question,
    col_names=col_names,
)
```

**输入示例**：
- `question`: "What was the total revenue in 2023?"
- `col_names`: "Year, Revenue, Profit, Product"

### 步骤 2：LLM Prompt 模板

**代码位置**：`code/my_agents.py:632-666`

**Prompt 模板**：
```
You are a task analyst for table question answering.
Given a question and table column names, analyze the semantic features of the question
and select which columns are needed to answer it.

Return ONLY a JSON object in the following format:
{
  "coarse_intent": "lookup|aggregation|comparison|ranking|trend|multi_step",
  "semantic_flags": {
    "has_aggregation": false,
    "has_comparison": false,
    "has_temporal_reasoning": false,
    "has_multi_step": false,
    "has_ranking": false,
    "num_constraints": 0
  },
  "selected_columns": ["col1", "col2"]
}

Notes:
- "coarse_intent": one of: lookup, aggregation, comparison, ranking, trend, multi_step.
- "semantic_flags": boolean/integer features describing the question's complexity:
  - "has_aggregation": true if question requires sum/average/count/max/min operations
  - "has_comparison": true if question requires comparing values
  - "has_temporal_reasoning": true if question involves time/year/trend analysis
  - "has_multi_step": true if question requires multiple reasoning steps
  - "has_ranking": true if question asks for top/bottom/ranking
  - "num_constraints": number of filter conditions in the question (0, 1, 2, ...)
- "selected_columns": List of column names needed to answer the question. Use exact column names.

[Question]
{question}

[Table Columns]
{col_names}
```

**关键点**：
- Prompt 明确告诉 LLM 需要输出哪些特征
- 每个特征都有明确的判断标准（如 "has_aggregation: true if question requires sum/average/count/max/min operations"）
- LLM 根据这些标准，分析问题文本，判断每个特征的真假

### 步骤 3：LLM 分析并输出 JSON

**代码位置**：`code/my_agents.py:730`

```python
# 调用 LLM 提取语义特征
raw = (self.llm_fn(prompt) or "").strip()  # LLM 原始输出
```

**LLM 输出示例**（对于问题 "What was the total revenue in 2023?"）：

```json
{
  "coarse_intent": "aggregation",
  "semantic_flags": {
    "has_aggregation": true,
    "has_comparison": false,
    "has_temporal_reasoning": true,
    "has_multi_step": false,
    "has_ranking": false,
    "num_constraints": 1
  },
  "selected_columns": ["Year", "Revenue"]
}
```

**LLM 的判断逻辑**（LLM 内部推理，我们看不到，但可以理解）：
- **has_aggregation = true**：因为问题包含 "total"（总和），需要 sum 操作
- **has_temporal_reasoning = true**：因为问题包含 "2023"（年份），涉及时间
- **num_constraints = 1**：因为有一个过滤条件（年份 = 2023）
- **has_comparison = false**：问题没有比较两个值
- **has_multi_step = false**：问题只需要一步（求和）
- **has_ranking = false**：问题没有排序/排名

### 步骤 4：解析 JSON 并提取特征

**代码位置**：`code/my_agents.py:737-760`

```python
data = _safe_json_loads(raw)  # 解析为 JSON

# 提取语义特征（semantic_flags）—— 这是 LLM 输出的核心
semantic_flags = data.get("semantic_flags", {}) if isinstance(data, dict) else {}
if not isinstance(semantic_flags, dict):
    semantic_flags = {}

# 提取各个特征值
has_agg = semantic_flags.get("has_aggregation", False)
has_cmp = semantic_flags.get("has_comparison", False)
has_temporal = semantic_flags.get("has_temporal_reasoning", False)
has_multi = semantic_flags.get("has_multi_step", False)
has_rank = semantic_flags.get("has_ranking", False)
num_constraints = semantic_flags.get("num_constraints", 0)
```

**解析结果示例**：
```python
semantic_flags = {
    "has_aggregation": True,
    "has_comparison": False,
    "has_temporal_reasoning": True,
    "has_multi_step": False,
    "has_ranking": False,
    "num_constraints": 1
}
```

### 步骤 5：使用公式计算 sem_score

**代码位置**：`code/my_agents.py:498-566`

```python
# ⭐ 核心改进：使用公式计算 sem_score（而不是 LLM 直接给分）
sem_score = compute_sem_score(semantic_flags, coarse_intent)
```

**公式计算**：
```python
base_score = (
    0.25 * has_agg +           # 0.25 * 1.0 = 0.25
    0.20 * has_cmp +           # 0.20 * 0.0 = 0.00
    0.20 * has_temporal +      # 0.20 * 1.0 = 0.20
    0.15 * has_multi +         # 0.15 * 0.0 = 0.00
    0.10 * has_rank +          # 0.10 * 0.0 = 0.00
    0.10 * tanh(num_constraints / 2)  # 0.10 * tanh(0.5) ≈ 0.046
)
# base_score = 0.25 + 0.00 + 0.20 + 0.00 + 0.00 + 0.046 = 0.496

# intent_bonus（coarse_intent = "aggregation"）
intent_bonus = 0.05

# 最终 sem_score
sem_score = 0.496 + 0.05 = 0.546
```

---

## 为什么用 LLM 而不是规则提取？

### 方案对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| **LLM 提取（当前方案）** | 1. 能理解自然语言的语义<br>2. 能处理复杂表达（如"总和"、"平均"、"最多"等）<br>3. 能识别隐含的语义（如"增长"隐含时间推理）<br>4. 适应性强，能处理新问题 | 1. 需要调用 LLM，有 token 消耗<br>2. 输出可能不稳定（但可以通过 prompt 约束） |
| **规则提取（备选方案）** | 1. 速度快，无需 LLM 调用<br>2. 输出稳定，可复现 | 1. 需要维护大量规则（如关键词列表）<br>2. 无法处理复杂表达和隐含语义<br>3. 规则可能遗漏某些情况 |

### 为什么选择 LLM 提取？

1. **语义理解能力强**：
   - 问题 "What was the total revenue?" → LLM 能识别 "total" 表示聚合
   - 问题 "How much did we make in 2023?" → LLM 能识别 "make" 在财务语境下表示收入，需要聚合
   - 规则提取需要维护大量同义词列表（total, sum, aggregate, 总和, 合计等）

2. **能处理复杂表达**：
   - 问题 "Which year had the highest growth rate?" → LLM 能识别：
     - `has_aggregation = true`（需要计算增长率）
     - `has_temporal_reasoning = true`（涉及年份）
     - `has_ranking = true`（"highest" 表示排序）
   - 规则提取很难同时识别多个特征

3. **适应性强**：
   - 新问题类型出现时，LLM 通常能正确识别
   - 规则提取需要不断更新规则库

4. **Token 消耗可控**：
   - 当前方案只传问题 + 列名，约 100-200 tokens
   - LLM 输出 JSON 格式，约 50-100 tokens
   - 总计约 150-300 tokens，相比后续路径选择节省的 token（数千到数万），成本很低

---

## LLM 判断标准的详细说明

### 1. has_aggregation（聚合操作）

**LLM 判断标准**（来自 prompt）：
> "has_aggregation": true if question requires sum/average/count/max/min operations

**LLM 会识别的关键词/表达**：
- **英语**：total, sum, average, mean, count, max, maximum, min, minimum, aggregate, cumulative
- **中文**：总和、合计、平均、最大值、最小值、累计、汇总

**示例**：
- "What is the total sales?" → `has_aggregation = true`（total = sum）
- "What is the average price?" → `has_aggregation = true`（average）
- "Which product has the highest revenue?" → `has_aggregation = true`（highest = max）

### 2. has_comparison（比较操作）

**LLM 判断标准**（来自 prompt）：
> "has_comparison": true if question requires comparing values

**LLM 会识别的关键词/表达**：
- **英语**：compare, higher, lower, more, less, better, worse, difference, than
- **中文**：比较、更高、更低、更多、更少、更好、更差、差异、比

**示例**：
- "Which team scored more points?" → `has_comparison = true`（more = 比较）
- "Is revenue in 2023 higher than 2022?" → `has_comparison = true`（higher than = 比较）

### 3. has_temporal_reasoning（时间推理）

**LLM 判断标准**（来自 prompt）：
> "has_temporal_reasoning": true if question involves time/year/trend analysis

**LLM 会识别的关键词/表达**：
- **英语**：year, month, quarter, before, after, last, previous, next, trend, growth, decline
- **中文**：年、月、季度、之前、之后、去年、前年、趋势、增长、下降

**示例**：
- "What was the revenue in 2023?" → `has_temporal_reasoning = true`（年份）
- "Which year had the highest growth?" → `has_temporal_reasoning = true`（年份 + 趋势）

### 4. has_multi_step（多步推理）

**LLM 判断标准**（来自 prompt）：
> "has_multi_step": true if question requires multiple reasoning steps

**LLM 会识别的模式**：
- 需要先计算中间结果，再基于中间结果计算最终答案
- 包含多个子问题（如"先算A，再算B，最后比较A和B"）

**示例**：
- "What is the average revenue per employee?" → `has_multi_step = true`（需要先算总营收，再除以员工数）
- "Which team won the most games in the last 3 years?" → `has_multi_step = true`（需要先筛选最近3年，再统计胜场，再排序）

### 5. has_ranking（排序操作）

**LLM 判断标准**（来自 prompt）：
> "has_ranking": true if question asks for top/bottom/ranking

**LLM 会识别的关键词/表达**：
- **英语**：top, bottom, first, last, rank, ranking, order, best, worst
- **中文**：最高、最低、第一、最后、排名、排序、最好、最差

**示例**：
- "Which team ranked first?" → `has_ranking = true`（first = 排序）
- "What are the top 3 products?" → `has_ranking = true`（top = 排序）

### 6. num_constraints（约束条件数量）

**LLM 判断标准**（来自 prompt）：
> "num_constraints": number of filter conditions in the question (0, 1, 2, ...)

**LLM 会识别的模式**：
- WHERE 子句的数量（如年份、产品、地区等过滤条件）

**示例**：
- "What is the revenue in 2023?" → `num_constraints = 1`（年份约束）
- "What is the revenue in 2023 for product A?" → `num_constraints = 2`（年份 + 产品约束）
- "What is the total revenue?" → `num_constraints = 0`（无约束）

---

## 实际运行示例

### 示例 1：简单查找问题

**问题**：`"What was the revenue in 2023?"`

**LLM 输出**：
```json
{
  "coarse_intent": "lookup",
  "semantic_flags": {
    "has_aggregation": false,
    "has_comparison": false,
    "has_temporal_reasoning": true,
    "has_multi_step": false,
    "has_ranking": false,
    "num_constraints": 1
  },
  "selected_columns": ["Year", "Revenue"]
}
```

**LLM 判断理由**：
- `has_temporal_reasoning = true`：包含年份 "2023"
- `num_constraints = 1`：有一个过滤条件（年份 = 2023）
- `has_aggregation = false`：只是查找，不需要聚合

**计算 sem_score**：
```python
base_score = 0.25*0 + 0.20*0 + 0.20*1 + 0.15*0 + 0.10*0 + 0.10*tanh(1/2)
           = 0.00 + 0.00 + 0.20 + 0.00 + 0.00 + 0.046
           = 0.246
sem_score = 0.246 + 0.00 = 0.246  # intent_bonus = 0（lookup）
```

### 示例 2：聚合问题

**问题**：`"What was the total revenue in 2023?"`

**LLM 输出**：
```json
{
  "coarse_intent": "aggregation",
  "semantic_flags": {
    "has_aggregation": true,
    "has_comparison": false,
    "has_temporal_reasoning": true,
    "has_multi_step": false,
    "has_ranking": false,
    "num_constraints": 1
  },
  "selected_columns": ["Year", "Revenue"]
}
```

**LLM 判断理由**：
- `has_aggregation = true`：包含 "total"（总和）
- `has_temporal_reasoning = true`：包含年份 "2023"
- `num_constraints = 1`：有一个过滤条件

**计算 sem_score**：
```python
base_score = 0.25*1 + 0.20*0 + 0.20*1 + 0.15*0 + 0.10*0 + 0.10*tanh(1/2)
           = 0.25 + 0.00 + 0.20 + 0.00 + 0.00 + 0.046
           = 0.496
sem_score = 0.496 + 0.05 = 0.546  # intent_bonus = 0.05（aggregation）
```

### 示例 3：复杂多步问题

**问题**：`"Which year had the highest average revenue per employee?"`

**LLM 输出**：
```json
{
  "coarse_intent": "multi_step",
  "semantic_flags": {
    "has_aggregation": true,
    "has_comparison": false,
    "has_temporal_reasoning": true,
    "has_multi_step": true,
    "has_ranking": true,
    "num_constraints": 0
  },
  "selected_columns": ["Year", "Revenue", "Employees"]
}
```

**LLM 判断理由**：
- `has_aggregation = true`：需要计算 "average"
- `has_temporal_reasoning = true`：涉及 "year"
- `has_multi_step = true`：需要先算平均收入/员工，再按年份分组，再排序
- `has_ranking = true`：需要找 "highest"（最高）

**计算 sem_score**：
```python
base_score = 0.25*1 + 0.20*0 + 0.20*1 + 0.15*1 + 0.10*1 + 0.10*tanh(0/2)
           = 0.25 + 0.00 + 0.20 + 0.15 + 0.10 + 0.00
           = 0.70
sem_score = 0.70 + 0.10 = 0.80  # intent_bonus = 0.10（multi_step）
```

---

## 总结

### 参数获取流程

```
问题文本 + 列名
    ↓
LLM Prompt（明确要求输出 semantic_flags）
    ↓
LLM 分析问题语义
    ↓
LLM 输出 JSON（包含 semantic_flags）
    ↓
代码解析 JSON
    ↓
提取各个特征值（has_aggregation, has_comparison等）
    ↓
使用公式 compute_sem_score() 计算 sem_score
```

### 关键点

1. **参数来源**：LLM 通过分析问题文本得到，不是代码自动提取
2. **判断标准**：Prompt 中明确告诉 LLM 每个特征的判断标准
3. **输出格式**：LLM 输出 JSON 格式，代码解析后提取特征值
4. **公式计算**：特征值提取后，使用公式计算最终的 sem_score

### 为什么这样设计？

- **利用 LLM 的语义理解能力**：能处理复杂表达和隐含语义
- **保持公式的可解释性**：特征值由 LLM 提取，但最终分数由公式计算
- **平衡准确性和可解释性**：LLM 负责"理解"，公式负责"计算"

---

**文档维护**：本文档会在 LLM prompt 优化后同步更新。

