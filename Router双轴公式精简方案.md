## Router 双轴难度建模精简方案（候选稿）

> 说明：这是在**不修改现有代码**前提下提出的一个“精简但更有研究味道”的 Router 公式设计方案。  
> 目的：供你从“是否足够成为研究生论文的第一个研究点”角度审阅。若你认可，再决定是否让代码与之对齐。

---

### 1. 研究目标与设计原则

- **研究目标**  
  为表格问答（Table QA）设计一个可解释、可消融的 **Router 双轴难度模型**，在简单路径（SIMPLE）与复杂路径（COMPLEX）之间做智能路由，并显式考虑：
  - 问题的 **语义推理复杂度**（Semantic Difficulty）  
  - 为回答问题所需的 **表格证据信息量 / 通信成本**（Structural / Communication Complexity）

- **设计原则**
  - **参数精简但有含义**：尽量减少自由参数个数，每一个都要有清晰的语义解释。
  - **LLM 只做粗判定，核心难度建模在 Router 端完成**：避免“把建模能力全部交给 LLM”。
  - **双轴结构清晰，可写成论文中的一个完整建模章节**，并支持消融实验。

为此，我们引入一个 **双轴难度向量**：

- 语义轴：\( \text{sem\_score} \in [0, 1] \)  
- 结构/通信轴：\( \text{cell\_score} \in [0, 1] \)

再将二者线性融合，得到最终的综合难度分数 \( \text{total\_score} \)，用于路由决策。

---

### 2. 语义轴 `sem_score` 的精简建模

#### 2.1 语义操作模式分层

从人类对表格问题的“操作视角”出发，我们将所有问题的语义复杂度抽象为三类操作模式：

1. **查值型（Lookup）**  
   - 只需在表中找到某一行某一列的值，或少量直接比对。  
   - 示例：  
     - “2010 年的 GDP 是多少？”  
     - “北京在表中的人口是多少？”

2. **局部聚合 / 对比型（Agg/Comp）**  
   - 需要在局部数据上做一次聚合或对比操作：求和、平均、最大值、差值、比例等。  
   - 示例：  
     - “近三年的平均增长率是多少？”  
     - “哪个城市的人均收入最高？”

3. **多步推理 / 趋势型（Multi/Trend）**  
   - 涉及多个子操作序列（筛选 → 聚合 → 比较 → 排序等），或沿时间维度分析趋势。  
   - 示例：  
     - “哪一年开始 A 省的增长率连续 3 年超过 B 省？”  
     - “在所有地区中，过去 5 年人均收入增幅最大的地区是哪一个？”

这三个模式本身就是一个 **“语义操作复杂度的阶梯结构”**：

\[
\text{Lookup} \;<\; \text{Agg/Comp} \;<\; \text{Multi/Trend}
\]

#### 2.2 LLM 输出的语义信号（极简版）

为了减少对 LLM 的依赖，我们只要求 LLM 输出 **三个高层语义信号**：

- **`coarse_intent`：粗粒度意图（被 Router 映射为 3 类）**
  - LLM 原始输出仍可为 `lookup / aggregation / comparison / ranking / trend / multi_step` 等标签；
  - Router 端统一映射为三类：
    - `lookup` → `lookup`
    - `{aggregation, comparison, ranking}` → `agg_or_comp`
    - `{multi_step, trend}` → `multi_or_trend`

- **`has_temporal_reasoning`：是否涉及时间/趋势**
  - `True / False`；
  - 用于识别“沿时间轴对比或分析”的额外复杂度。

> 这样，LLM 的任务从“输出很多细碎的 flag”简化为“判断问题属于哪一类操作模式 + 是否涉及时间”，难度显著降低，主导权回到 Router 的公式上。

#### 2.3 基于意图的分段基础分

我们为三类语义操作模式定义一个 **基础难度分数**：

- \( b_{\text{lookup}} = 0.1 \)
- \( b_{\text{agg}} = 0.4 \)
- \( b_{\text{multi}} = 0.7 \)

其中：

\[
 b_{\text{intent}} =
 \begin{cases}
   0.1, & \text{if intent} = \text{lookup} \\
   0.4, & \text{if intent} = \text{agg\_or\_comp} \\
   0.7, & \text{if intent} = \text{multi\_or\_trend} \\
   0.4, & \text{otherwise (fallback)}
 \end{cases}
\]

**设计动机（可直接写进论文）：**

- 查值型问题本质上是单步定位操作；但考虑到自然语言理解本身仍有复杂度，因此设为 0.1 而不是 0。
- 聚合/对比型问题需要在局部区域做运算，属于“单阶段多 cell 操作”，比查值显著复杂，因此设在中段 0.4。
- 多步/趋势型问题往往对应“操作序列长度 ≥ 3”或跨时间分析，属于最高级别的语义复杂度，因此设为高段 0.7。

这三类基础分可用一张简单的柱状图展示，直观体现“操作模式阶梯”。

#### 2.4 时间维度的加成项

在基础分之上，我们引入一个简单的 0/1 加成项，用于捕捉时间相关问题的额外复杂度：

**时间/趋势加成 \( \delta_{\text{temporal}} \)**

- 若 `has_temporal_reasoning = True` 且 `intent != lookup`，说明问题不仅是聚合/多步，还牵涉“沿时间轴”的对比或趋势分析：
  - 设 \( \delta_{\text{temporal}} = 0.1 \)

- **设计动机（可直接写进论文）：**  
  时间维度上的分析通常意味着需要对多个年份的数值进行排序、差分或趋势判断，这比同一时间截面的横向对比更复杂。例如：
  - “近三年平均增长率”需要先提取三年数据，再计算增长率，再平均；
  - “哪一年开始连续超过”需要沿时间轴做序列比较。
  
  因此，在非查值类问题中，若涉及时间维度，我们给予一个小的难度加成。

#### 2.5 完整 `sem_score` 公式

综上，语义难度分数定义为：

\[
\text{sem\_score} = \text{clip}\left(
 b_{\text{intent}} +
 \mathbf{1}_{\text{temporal}} \cdot \delta_{\text{temporal}},
 0, 1
\right)
\]

其中：

- \( b_{\text{intent}} \in \{0.1, 0.4, 0.7\} \)
- \( \delta_{\text{temporal}} = 0.1 \)
- `clip(·, 0, 1)` 表示将结果截断在 \([0, 1]\) 区间。
- \( \mathbf{1}_{\text{temporal}} \) 是指示函数：当 `has_temporal_reasoning = True` 且 `intent != lookup` 时为 1，否则为 0。

**参数总数（语义轴）：4 个。**

- 3 个基础分参数：\( b_{\text{lookup}}, b_{\text{agg}}, b_{\text{multi}} \)
- 1 个加成参数：\( \delta_{\text{temporal}} \)

每个参数都有清晰的含义，便于在论文中解释和作图。

---

### 3. 结构/通信轴 `cell_score` 的建模

结构/通信轴关注的是：**为回答当前问题，系统需要查看多少表格证据信息**。  
这一轴与 LLM 语义理解能力无关，完全由 Router 和表格本身决定。

#### 3.1 证据信息量刻画：单元格比例

设：

- 原始表格规模：  
  \[
    N_{\text{orig}} = \text{rows} \times \text{cols}
  \]
- 压缩后实际用于推理的表格规模：  
  \[
    N_{\text{used}} = \text{rows\_used} \times \text{cols\_used}
  \]

我们定义 **压缩比例**：

\[
\text{cell\_ratio} = \frac{N_{\text{used}}}{N_{\text{orig}}}
\]

- 当只用到原表的一小部分时，`cell_ratio` 趋近 0；
- 当几乎没法压缩时，`cell_ratio` 趋近 1。

#### 3.2 表结构类型权重

不同结构类型的表，对 LLM 的“阅读和对齐成本”不同。我们引入一个结构类型权重：

- 单轴表（只有列头）：`single_axis`  
  - 结构简单，权重 \( w_{\text{single}} = 1.0 \)（基准）

- 双轴表（既有列头，也有明显的行头/索引列）：`double_axis`  
  - 行列交叉语义更丰富，权重 \( w_{\text{double}} = 1.2 \)

- 多级表头表（多行表头或合并单元格）：`multi_header`  
  - 解析复杂、层次多，权重 \( w_{\text{multi}} = 1.5 \)

这些权重由 `TableStructureAnalyzer` 根据 DataFrame 统计与启发式规则确定。

**为什么权重可以超过 1.0？**

- 权重超过 1.0 的设计意图是：**相同压缩比例下，结构更复杂的表应该被“放大”其通信复杂度**。
- 例如：如果 `cell_ratio = 0.8`（压缩后用了原表的 80%），那么：
  - 单轴表：`cell_score = 0.8 × 1.0 = 0.8`
  - 双轴表：`cell_score = 0.8 × 1.2 = 0.96`
  - 多级表头：`cell_score = 0.8 × 1.5 = 1.2` → **被 clip 到 1.0**
- 这意味着：**多级表头即使压缩到 80%，其复杂度仍然达到上限（1.0）**，体现了“结构复杂度的放大效应”。
- 最终公式中的 `clip(·, 0, 1)` 确保 `cell_score` 始终在 \([0, 1]\) 区间内，不会因为权重超过 1.0 而超出范围。

#### 3.3 完整 `cell_score` 公式

结构/通信复杂度分数定义为：

\[
\text{cell\_score} = \text{clip}\left(
 \text{cell\_ratio} \cdot w_{\text{structure}},
 0, 1
\right)
\]

其中：

- \( \text{cell\_ratio} = \dfrac{N_{\text{used}}}{N_{\text{orig}}} \)
- \( w_{\text{structure}} \in \{1.0, 1.2, 1.5\} \)

**参数总数（结构轴）：3 个结构权重。**

该公式天然满足直觉：

- 使用越少单元格，`cell_ratio` 越小 → `cell_score` 越低，代表“通信成本低，适合简单路径或复杂路径但压缩后开销可控”；
- 相同 `cell_ratio` 下，多级表头/双轴表由于结构复杂，`cell_score` 更高；
- 通过 Router 压缩表格，`cell_ratio` 和 `cell_score` 都会下降，体现出 Router 对后续模块的通信优化贡献。

---

### 4. 双轴融合与路由决策

#### 4.1 综合难度分数 `total_score`

为了将语义轴与结构轴统一成一个标量难度度量，我们采用简单的线性融合：

\[
\text{total\_score} = w_{\text{sem}} \cdot \text{sem\_score} +
                      w_{\text{cell}} \cdot \text{cell\_score}
\]

默认参数：

- \( w_{\text{sem}} = 0.6 \)（语义轴权重）
- \( w_{\text{cell}} = 0.4 \)（结构/通信轴权重）

设计考虑：

- 对于同一个表，问题语义难度是“是否需要复杂推理”的主要来源，因此语义轴权重略高；
- 结构/通信轴则更偏向“是否值得走复杂路径”的成本考量。

这两个权重也可以作为实验中的调节参数，研究：

- 更偏语义（\( w_{\text{sem}} \) 增大） vs 更偏通信成本（\( w_{\text{cell}} \) 增大）时，路由策略和整体性能如何变化。

#### 4.2 难度等级划分

根据综合难度分数，我们定义一个简单的二分难度等级：

\[
\text{difficulty\_level} =
\begin{cases}
 \text{easy}, & \text{if } \text{total\_score} < \tau \\
 \text{hard}, & \text{otherwise}
\end{cases}
\]

其中阈值 \( \tau \) 默认为：

- \( \tau = 0.5 \)

这个阈值可以通过验证集调优，并在论文中展示“不同阈值下 SIMPLE/COMPLEX 样本比例及准确率的变化曲线”。

#### 4.3 SIMPLE / COMPLEX 路由规则

最终的路由决策结合 **难度等级** 与 **语义意图**：

- 若 `difficulty_level = "easy"` 且 `intent = "lookup"`：  
  - **路由：SIMPLE**  
  - 理由：语义复杂度低，且结构开销可控，简单路径足以。

- 若 `difficulty_level = "hard"` 或 `intent = "multi_or_trend"`：  
  - **路由：COMPLEX**  
  - 理由：要么综合难度本身已高，要么问题类型本质就需要多步推理/趋势分析，适合走 Planner + Calculator + Critic 的强推理链。

这一规则形成了一个可视化的“难度平面”：

- 横轴：`sem_score`
- 纵轴：`cell_score`
- 平面上存在一条由 \( w_{\text{sem}}, w_{\text{cell}}, \tau \) 决定的“分界线”，将 SIMPLE / COMPLEX 样本分开。

---

### 5. 参数数量与“研究点强度”分析

#### 5.1 参数总数统计

- **语义轴参数（4 个）**
  - \( b_{\text{lookup}}, b_{\text{agg}}, b_{\text{multi}} \)
  - \( \delta_{\text{temporal}} \)

- **结构轴参数（3 个）**
  - 结构权重：\( w_{\text{single}}, w_{\text{double}}, w_{\text{multi}} \)

- **融合与阈值（3 个）**
  - 融合权重：\( w_{\text{sem}}, w_{\text{cell}} \)
  - 难度阈值：\( \tau \)

**核心自由度总计：\( 4 + 3 + 3 = 10 \) 个。**

这些参数：

- 数量不多，且分布在三个层级（语义、结构、融合），利于解释；
- 每一个都有可理解的功能定位，便于论文中单独分析与消融。

#### 5.2 论文层面的“研究点强度”

从毕业论文第一研究点的角度，这套方案具备以下特点：

- 有明确的 **任务动机**：在 Table QA 中，直接把所有问题扔给复杂路径会浪费大量 token；因此需要一个“任务特征感知的 Router”。
- 有结构化的 **理论建模**：
  - 语义操作模式的分层与数值映射；
  - 证据信息量/通信成本的 cell_ratio + 结构权重建模；
  - 双轴融合与分界线的形式化表达。
- 有 **参数可解释性与可实验性**：
  - 每个参数可做敏感性分析与消融实验；
  - 可比较不同配比（例如偏语义 vs 偏结构）对路由与最终精度/token 消耗的影响。
- 工程实现上不复杂，**但理论部分可以写出一个完整章节**，包括：
  - 模型假设与动机；
  - 数学公式与参数设定；
  - 可视化（难度平面、直方图等）；
  - 与 baseline（不分轴的简单规则、纯 LLM 打分等）的对比。

---

### 6. 与现有实现的关系与迁移建议（非必须）

当前代码中的实现已经具备：

- `sem_score` / `cell_score` / `total_score` 三轴/两轴结构；
- `TableStructureAnalyzer`、Router 压缩表格、DifficultyScorer 等模块。

若你最终认可本方案，可以考虑后续步骤：

- 将 `FeatureExtractor` 的输出精简为本方案中的 2 个信号（`coarse_intent` + `has_temporal_reasoning`）；
- 将 `compute_sem_score` 替换为本方案的分段 + 加成公式；
- 保持 `cell_score` 公式基本不变，仅在文档中以本方案的方式重新组织解释；
- 在 `目前工程.md` 中同步更新 Router 建模部分，使其与本方案一致；
- 在论文/专利文稿中，以本方案为主线撰写“任务特征感知 Router 的双轴难度建模方法”章节。

上述迁移不影响整个系统的总体结构，只是让 Router 的理论部分更加统一、精简且“学术味更浓”。

---

### 7. 你后续可以关注与反馈的问题

在你评估这个方案是否满足“研究生论文第一个研究点”时，可以重点考虑：

- 这种“3 类操作模式 + 1 个时间加成 + 双轴融合”的结构，是否符合你对**故事完整度**的预期？
- 语义轴参数（4 个）的数量和含义，你是否觉得足够简洁且易于解释？
- 结构轴用 cell_ratio × 结构权重的方式，是否能支撑你对“通信成本优化”的故事？
- 是否需要在语义轴再稍作扩展（例如单独区分 ranking），还是保持当前极简设计更好？

你也可以在这个方案基础上提出你想要增加或删除的部分，我可以再帮你调整到最符合你直觉和论文叙事的版本。

