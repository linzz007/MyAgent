# 目前工程实现文档（双轴难度建模版）

> 本文档实时记录表格问答系统各模块的当前实现逻辑和设计细节。
> **核心升级**：Router 模块采用"双轴难度建模"算法，所有分数由公式计算，可解释、可消融。

最后更新时间：2026年1月7日

---

## 目录

1. [项目概述](#项目概述)
2. [整体架构](#整体架构)
3. [核心算法：双轴难度建模](#核心算法双轴难度建模) ⭐⭐
4. [核心模块详解](#核心模块详解)
5. [数据流与状态管理](#数据流与状态管理)
6. [Token消耗优化策略](#token消耗优化策略)
7. [关键设计决策](#关键设计决策)
8. [实验与消融设计](#实验与消融设计)

---

## 项目概述

### 项目目标

本项目实现了一个**双轴难度建模的表格问答 Router**，核心目标是：
- **提高表格数据获取的准确性**
- **大幅减少LLM调用的token消耗**（目标：减少95%+）

### 核心创新点（论文/专利）

1. **双轴难度建模**：语义轴（sem_score）+ 结构/通信轴（cell_score）
2. **公式化计算**：所有分数由明确公式计算，可解释、可消融、可复现
3. **表结构感知**：支持单轴表、双轴表、多级表头，不同结构有不同权重
4. **自适应表压缩**：根据问题语义选择需要的列，压缩表格减少通信消耗

---

## 整体架构

### 系统架构图

```
用户输入 (Q, T)
    ↓
[InputHandler] 输入管理与格式统一
    ↓
[Router模块] ⭐⭐ 核心模块：双轴难度建模
    │
    ├─→ [TableStructureAnalyzer] 表结构分析
    │       └─→ 输出: table_type, structure_weight
    │
    ├─→ [FeatureExtractor] 语义特征提取
    │       ├─→ LLM 输出: semantic_flags, selected_columns
    │       └─→ 公式计算: sem_score = f(semantic_flags)
    │
    ├─→ [表格压缩] 根据 selected_columns 压缩表格
    │       └─→ 计算: cell_ratio = compressed / original
    │
    ├─→ [DifficultyScorer] 综合难度评分
    │       ├─→ 公式计算: cell_score = g(cell_ratio, structure_weight)
    │       └─→ 公式计算: total_score = α×sem + (1-α)×cell
    │
    └─→ 路径决策 (SIMPLE/COMPLEX)
    ↓
    ├─→ SIMPLE路径
    │       ↓
    │   [SimplePathAgent] 简单路径推理
    │       ↓
    │   [FinalAnswerAgent] 生成最终答案
    │
    └─→ COMPLEX路径
            ↓
        [Planner] 复杂路径规划
            ↓
        [Calculator] 代码执行
            ↓
        [LightCritic] 轻量校验
            ↓ (如果REPLAN，返回Planner)
        [FinalAnswerAgent] 生成最终答案
```

### 模块列表

| 模块名称 | 文件位置 | 职责 | 核心算法 |
|---------|---------|------|---------|
| InputHandler | `my_agents.py` | 输入预处理和格式统一 | - |
| **TableStructureAnalyzer** ⭐ | `my_agents.py` | 表结构类型识别 | 规则分析 |
| **FeatureExtractor** ⭐ | `my_agents.py` | 语义特征提取 + sem_score | LLM + 公式 |
| **DifficultyScorer** ⭐ | `my_agents.py` | 综合难度评分 | 公式计算 |
| **RouterAgent** ⭐⭐ | `my_agents.py` | 双轴建模 + 路由决策 | 核心算法 |
| SimplePathAgent | `my_agents.py` | 简单路径推理 | 单次 LLM |
| PlannerAgent | `my_agents.py` | 复杂路径规划 | LLM 生成 |
| Calculator | `my_agents.py` | 代码执行 | Python exec |
| CriticAgent | `my_agents.py` | 轻量校验 | 规则 + LLM |
| FinalAnswerAgent | `my_agents.py` | 最终答案生成 | 格式化 |

---

## 核心算法：双轴难度建模 ⭐⭐

> **这是本项目的核心创新点**：通过双轴难度建模实现任务特征感知的路径选择。
> 所有分数由明确公式计算，可解释、可消融、可复现。

### 1. 表结构分析（TableStructureAnalyzer）

**功能**：识别表格的结构类型，为 cell_score 计算提供结构权重。

**支持的表结构类型**：

| 类型 | 说明 | 结构权重 |
|------|------|---------|
| `single_axis` | 单轴表（只有列头） | 1.0 |
| `double_axis` | 双轴表（有列头和行索引） | 1.2 |
| `multi_header` | 多级表头表 | 1.5 |

**识别规则**：
- 检测第一列是否为行索引（唯一值比例高、非数值型）
- 检测列名是否包含层级分隔符（`/`、`|`、`-`）
- 检测是否有汇总行/汇总列（包含"合计"、"总计"等关键词）

### 2. 语义难度分数 sem_score（核心公式之一）⭐

**定义**：衡量问题在语义层面的复杂度，反映问题的推理难度。

**计算方式**：
- **旧版本（已废弃）**：LLM 直接输出 `semantic_score` 数值
- **新版本（当前）**：LLM 输出语义特征，由公式计算 sem_score

**语义特征（semantic_flags）**：

| 特征名 | 类型 | 说明 |
|--------|------|------|
| `has_aggregation` | bool | 是否有聚合操作（sum/avg/max/min） |
| `has_comparison` | bool | 是否有比较操作 |
| `has_temporal_reasoning` | bool | 是否有时间/趋势推理 |
| `has_multi_step` | bool | 是否需要多步推理 |
| `has_ranking` | bool | 是否有排序操作 |
| `num_constraints` | int | 约束条件数量 |

**核心公式**：

```python
sem_score = (
    0.25 × has_aggregation +
    0.20 × has_comparison +
    0.20 × has_temporal_reasoning +
    0.15 × has_multi_step +
    0.10 × has_ranking +
    0.10 × tanh(num_constraints / 2)
)

# 根据 coarse_intent 调整
if coarse_intent in {"multi_step", "trend"}:
    sem_score += 0.10
elif coarse_intent in {"aggregation", "comparison"}:
    sem_score += 0.05

# 约束到 [0, 1]
sem_score = max(0, min(1, sem_score))
```

**分数含义**：
- **0.0 - 0.3**：简单语义（单步查询，无需复杂推理）
- **0.3 - 0.6**：中等语义（需要2-3步推理，涉及简单运算）
- **0.6 - 1.0**：复杂语义（需要多步推理，涉及复杂计算）

### 3. 结构/通信复杂度分数 cell_score（核心公式之二）⭐

**定义**：衡量问题需要触达的表格单元格比例，反映数据范围需求和结构复杂度。

**计算方式**：

```python
# 1. 计算压缩比例
cell_ratio = compressed_cells / original_cells

# 2. 获取表结构权重
structure_weight = {
    "single_axis": 1.0,
    "double_axis": 1.2,
    "multi_header": 1.5
}[table_type]

# 3. 核心公式
cell_score = min(1.0, cell_ratio × structure_weight)
```

**公式解读**：
- `cell_ratio`：压缩后单元格数 / 原始单元格数，反映数据范围
- `structure_weight`：表结构权重，双轴/多级表头结构更复杂，即使 cell_ratio 相同，难度也更高

**分数含义**：
- **0.0 - 0.3**：小范围数据（只需少量单元格）
- **0.3 - 0.6**：中等范围数据（需要部分数据）
- **0.6 - 1.0**：大范围数据（需要大部分或全部数据）

### 4. 综合难度分数 total_score（核心公式之三）⭐

**计算方式**：加权线性组合

```python
total_score = α × sem_score + (1 - α) × cell_score

# 默认权重：α = 0.6
# 即：语义难度占 60%，结构复杂度占 40%
```

**难度等级划分**：

```python
if total_score < 0.5:
    difficulty_level = "easy"   # 简单路径
else:
    difficulty_level = "hard"   # 复杂路径
```

### 5. 路由决策规则

```python
# 复杂意图直达复杂路径
if coarse_intent in {"aggregation", "comparison", "ranking", "trend", "multi_step"}:
    route = "COMPLEX"
# 根据难度等级决定
elif difficulty_level == "easy":
    route = "SIMPLE"
else:
    route = "COMPLEX"
```

---

## 核心模块详解

### 1. TableStructureAnalyzer（表结构分析器）

**文件位置**：`code/my_agents.py`

**职责**：
- 分析表格结构类型（单轴/双轴/多级表头）
- 输出结构权重，用于 cell_score 计算

**核心方法**：`analyze(df) -> Dict`

**输出格式**：
```python
{
    "table_type": "single_axis",      # 表结构类型
    "structure_weight": 1.0,          # 结构权重
    "n_rows": 100,                    # 行数
    "n_cols": 10,                     # 列数
    "has_summary_row": False,         # 是否有汇总行
    "is_first_col_index": False,      # 第一列是否为索引
}
```

### 2. FeatureExtractor（语义特征抽取器）

**文件位置**：`code/my_agents.py`

**职责**：
- 通过 LLM 提取语义特征（semantic_flags）
- 使用公式 `compute_sem_score()` 计算 sem_score
- 获取需要的列信息（selected_columns）

**核心方法**：`extract(question, table_schema) -> Tuple`

**LLM 输入**：问题 + 列名（精简输入，减少 token）

**LLM 输出格式**：
```json
{
  "coarse_intent": "lookup",
  "semantic_flags": {
    "has_aggregation": false,
    "has_comparison": false,
    "has_temporal_reasoning": true,
    "has_multi_step": false,
    "has_ranking": false,
    "num_constraints": 1
  },
  "selected_columns": ["Year", "League"]
}
```

**输出**：
```python
(
    sem_score: float,           # 语义难度分数（公式计算）⭐
    semantic_flags: Dict,       # 语义特征（LLM 输出）
    structural_hints: Dict,     # 结构提示（兼容性保留）
    coarse_intent: str,         # 粗粒度意图
    selected_columns: List,     # 选择的列
    row_filter: Dict            # 行筛选（简化后默认为空）
)
```

### 3. DifficultyScorer（难度评分器）

**文件位置**：`code/my_agents.py`

**职责**：组合 sem_score 和 cell_score，计算综合难度分数

**核心方法**：`score(sem_score, cell_score) -> Tuple[float, str]`

**参数配置**：
- `w_sem = 0.6`：语义分数权重
- `w_cell = 0.4`：结构复杂度分数权重
- `threshold = 0.5`：难度划分阈值

### 4. RouterAgent（路由代理）

**文件位置**：`code/my_agents.py`

**职责**：整合所有子模块，执行双轴难度建模和路由决策

**核心方法**：`route(state) -> TQASessionState`

**执行流程**：
1. 调用 `TableStructureAnalyzer.analyze()` 分析表结构
2. 调用 `FeatureExtractor.extract()` 提取语义特征，计算 sem_score
3. 调用 `_reduce_df()` 压缩表格
4. 调用 `compute_cell_score()` 计算 cell_score
5. 调用 `DifficultyScorer.score()` 计算综合难度
6. 根据规则进行路由决策

**输出（保存到 state）**：
- `sem_score`：语义难度分数
- `cell_score`：结构复杂度分数
- `difficulty_score`：综合难度分数
- `difficulty_level`：难度等级
- `route_type`：路由类型（SIMPLE/COMPLEX）
- `routing_context`：完整路由上下文（便于调试和消融）

---

## 数据流与状态管理

### TQASessionState（会话状态对象）

**核心字段**：

```python
# 输入数据
question: str                      # 用户问题
df: pd.DataFrame                  # 当前表格（Router模块压缩后）
table_schema: Dict                 # 表格schema

# Router模块输出（双轴难度建模）⭐
route_type: Optional[str]          # "SIMPLE" 或 "COMPLEX"
sem_score: float                   # ⭐ 语义难度分数（公式计算）
cell_score: float                  # ⭐ 结构复杂度分数（公式计算）
difficulty_score: Optional[float]  # 综合难度分数
difficulty_level: Optional[str]    # "easy" 或 "hard"
coarse_intent: Optional[str]       # 粗意图标签
semantic_features: Dict            # 语义特征（LLM 输出）
structural_features: Dict          # 结构特征（包含 table_type）
routing_context: Dict              # 完整路由上下文

# 规划和执行信息
plan_steps: List[str]              # 规划步骤列表
code_str: str                      # 可执行代码
exec_success: bool                 # 执行是否成功
final_value: Any                   # 最终数值结果

# 最终答案
final_answer: Optional[str]        # 最终自然语言答案
```

---

## Token消耗优化策略

### 优化策略总结

| 优化策略 | 实现位置 | 优化效果 |
|---------|---------|---------|
| **精简 LLM 输入** | FeatureExtractor | 只传问题+列名，减少 80% 输入 token |
| **表格压缩** | RouterAgent | 压缩表格，减少 97%+ 后续 token |
| **路径选择** | RouterAgent | 简单问题走 SimplePath，减少 98.9% |

### Token消耗对比

**MACT基准（复杂问题）**：~48,500 tokens

**本项目（假设70%简单，30%复杂）**：
- 简单问题（70%）：Router(1,500) + SimplePath(550) + FinalAnswer(400) = 2,450 tokens
- 复杂问题（30%）：Router(1,500) + Planner(1,200) + Calculator(0) + Critic(1,300) + FinalAnswer(600) = 4,600 tokens
- **平均：0.7 × 2,450 + 0.3 × 4,600 = 3,095 tokens**

**优化效果**：
- **减少比例：约93.6%的token节省**

---

## 关键设计决策

### 1. 为什么用公式计算分数而不是 LLM 直接给分？

**原因**：
- **可解释性**：每个分数的来源清晰，可以追溯到具体特征
- **可消融性**：可以去掉某个特征，观察对性能的影响
- **稳定性**：公式计算结果稳定，不受 LLM 随机性影响
- **可复现性**：同样的输入，必然得到同样的分数

### 2. 为什么 cell_score 要考虑表结构类型？

**原因**：
- 双轴表、多级表头的结构更复杂，即使 cell_ratio 相同，实际处理难度也更高
- 结构权重可以反映这种差异，使 cell_score 更准确

### 3. 为什么 sem_score 权重（0.6）高于 cell_score（0.4）？

**原因**：
- 语义复杂度对路由决策的影响更大
- 一个语义复杂的问题，即使只涉及少量数据，也可能需要复杂路径
- 但这个权重可以根据实验调整

---

## 实验与消融设计

### 1. 对比实验

| 实验组 | 说明 |
|--------|------|
| 无 Router | 所有问题走复杂路径 |
| 仅 sem_score | 只用语义难度路由 |
| 仅 cell_score | 只用结构复杂度路由 |
| 双轴 Router | 完整方案 |

### 2. 消融实验

| 消融项 | 说明 |
|--------|------|
| 去掉 has_aggregation | 观察聚合特征的影响 |
| 去掉 has_temporal | 观察时间推理特征的影响 |
| 去掉 structure_weight | 观察表结构权重的影响 |
| 调整 α 权重 | 观察 sem/cell 权重比例的影响 |

### 3. 评估指标

- **准确率**：最终答案的正确率
- **Token 消耗**：平均每个问题的 token 消耗
- **路由准确率**：路由决策是否正确（需要人工标注）

---

**文档维护**：本文档会在每次代码优化后同步更新，确保反映最新的实现逻辑。
